export default class Calculator {

    private calculate(tables: object, baranceValue: number) {

        console.log(tables)
    }

    public main(tables: object, baranceValue: number) {

        this.calculate(tables, baranceValue)
    }
}